{
  "name": "takeitesee-coming-soon",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "eslint . --ext .js",
    "format": "prettier --write '**/*.{js,css,html,json,md}'"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^2.0.0"
  }
}
